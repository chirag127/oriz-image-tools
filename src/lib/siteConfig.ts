import type { OrizSiteConfig } from '@chirag127/oriz-ui'

export const SITE_CONFIG: OrizSiteConfig = {
  slug: 'image',
  name: 'Image Tools',
  origin: 'https://image.oriz.in',
  tagline: 'Compress, convert, resize — runs in your browser',
  description:
    'Free, privacy-first image tools. Compress, resize, crop, convert, watermark, upscale, remove backgrounds, blur faces — every operation runs locally in your browser. Zero uploads.',
}
